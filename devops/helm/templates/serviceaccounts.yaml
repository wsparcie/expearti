apiVersion: v1
kind: ServiceAccount
metadata:
  name: budzetownik-frontend-sa
  namespace: budzetownik-dev
  labels:
    app: budzetownik
    tier: frontend
    environment: dev
  annotations:
    description: 'Service account for budzetownik frontend application in development'
    contact: 'wsparcie@budzetownik.com'
automountServiceAccountToken: true
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: budzetownik-backend-sa
  namespace: budzetownik-dev
  labels:
    app: budzetownik
    tier: backend
    environment: dev
  annotations:
    description: 'Service account for budzetownik backend API in development'
    contact: 'wsparcie@budzetownik.com'
automountServiceAccountToken: true
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: budzetownik-database-sa
  namespace: budzetownik-dev
  labels:
    app: budzetownik
    tier: database
    environment: dev
  annotations:
    description: 'Service account for budzetownik database in development'
    contact: 'wsparcie@budzetownik.com'
automountServiceAccountToken: true
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: budzetownik-frontend-sa
  namespace: budzetownik-prod
  labels:
    app: budzetownik
    tier: frontend
    environment: prod
  annotations:
    description: 'Service account for budzetownik frontend application in production'
    contact: 'wsparcie@budzetownik.com'
automountServiceAccountToken: true
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: budzetownik-backend-sa
  namespace: budzetownik-prod
  labels:
    app: budzetownik
    tier: backend
    environment: prod
  annotations:
    description: 'Service account for budzetownik backend API in production'
    contact: 'wsparcie@budzetownik.com'
automountServiceAccountToken: true
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: budzetownik-database-sa
  namespace: budzetownik-prod
  labels:
    app: budzetownik
    tier: database
    environment: prod
  annotations:
    description: 'Service account for budzetownik database in production'
    contact: 'wsparcie@budzetownik.com'
automountServiceAccountToken: true
